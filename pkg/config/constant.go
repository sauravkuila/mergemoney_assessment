package config

// request constants
const (
	USERID        = "userId"
	MOBILE        = "mobile"
	REQUESTID     = "requestID"
	UCC           = "ucc"
	AUTHORIZATION = "Authorization"
	SCOPE         = "scope"
)

// config file constants
const (
	CONFIG_SERVER = "server"
)
